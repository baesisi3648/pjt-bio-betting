# 구현 검증 리포트 — 라운드 1

- 일자: 2026-08-01
- 대상: `apps-script/` 8개 파일 (약 1,700줄)
- 기준: `docs/planning/loop/08-derived-gates.md`

> ⚠️ **독립성 한계를 먼저 밝힙니다.** 이 세션은 서브에이전트 자동 호출이 꺼져 있어, 빌드와 검증을
> 같은 세션이 수행했습니다. 감독 프로토콜이 요구하는 "구현자 ≠ 검증자" 분리가 지켜지지 않았습니다.
> 대신 **판정을 진술이 아니라 실행 가능한 스크립트**로 만들어, 누구든 다시 돌려 확인할 수 있게 했습니다.
>
> ```bash
> node test/run-gates.js      # 17개
> node test/simulate-game.js  # 14개
> ```

---

## 결과 요약

| 게이트 묶음 | 통과 | 실패 |
|---|---|---|
| 게임 규칙 (`run-gates.js`) | **17** | 0 |
| 통합 시뮬레이션 (`simulate-game.js`) | **14** | 0 |
| **합계** | **31** | **0** |

---

## Hard 게이트

| ID | 게이트 | 결과 | 증거 |
|---|---|---|---|
| **H1** | 최종 도착 순서 = 정답 순위 | ✅ | 판 100개 전부 굴려 `100/100 일치` |
| **H1c** | 통합 시뮬레이션에서도 일치 | ✅ | `도착 DBAEGHCF vs 정답 DBAEGHCF` |
| **H2** | 모든 힌트가 참 | ✅ | `1800개 검사, 거짓 0개` |
| **H3-a** | 전체 힌트로 1·2·3등 유일 결정 | ✅ | `20/20` (8! 전수 필터) |
| **H3-b** | 어려움 6개만으로 유일 결정 | ✅ | `20/20` |
| **H4** | 모둠 응답에 정답 유출 없음 | ✅ | `유출 0건` — truth·moves·lastRound·타 모둠 pin·타 모둠 힌트 전부 미포함 |
| **H4b/c/d** | 정산 후에만 정답 공개 | ✅ | 정산 전 `0회 등장`, 정산 후 포함 |
| **H5** | 라운드 중간 캐시 소실 복구 | ✅ | 캐시 강제 삭제 후 `코인 8,8,8,8,8,8 / 힌트 6,6,6,6,0,0` 그대로 복구 (**손실 0**) |
| **H8** | 재접속·다중 기기 | ✅ | 같은 모둠 두 번째 기기 입장 성공 (점유 없음) |
| **H9** | 정산 계산 | ✅ | `4 × 2.14 × 0.7 = 5.99 → 6코인`, 최종 14 — 손계산 일치 |
| **H9c** | 최종 = 남은 보유 + 획득 | ✅ | 전 모둠 일치 |
| **H10** | 학교망 접속 | ⛔ **미검증** | 실제 배포·학교 와이파이가 있어야 확인 가능 |

## Domain 게이트

| ID | 게이트 | 결과 | 증거 |
|---|---|---|---|
| **D1** | 3라운드까지 골인 0마리 | ✅ | 판 100개 `0마리` |
| **D2** | 베팅 규칙을 서버가 막는다 | ✅ | `TOO_MANY_COINS / NOT_ENOUGH_COINS / ALREADY_BET` 전부 발동 |
| **D3** | 토론 단계에 베팅 잠김 | ✅ | 토론 중 베팅 → `BET_CLOSED` |
| **D5** | 캐시 복구 후에도 같은 문제 | ✅ | `questionPlan` 미리 배정, 6라운드 서로 다른 문항 6/6 |
| **D5b** | 문항 부족 시 순환 재사용 | ✅ | 2문항으로 6라운드 → `1,2,1,2,1,2` |
| **D6** | 같은 힌트 두 번 안 감 | ✅ | 6라운드 내내 어려움 고른 모둠도 중복 0 |
| **D9** | 외부 요청 0건 | ✅ | CDN·웹폰트·외부 API 참조 없음 (`system-ui` 계열만) |
| **D10** | 정산 전 등수 배지 없음 | ✅ | `Teacher.html` 트랙에 골인 표시만, 등수 미표기 |
| **D11** | 학생 개인정보 미저장 | ✅ | 이름·학번 입력란 없음. 모둠 이름과 4자리 암호만 |
| **D4** | 같은 난이도 = 같은 문제 | ✅ | `questionPlan[round][level]` 단일 값 |
| **D7** | 일시정지 후 남은 시간 유지 | ⚠️ **부분** | 코드는 구현됨(`gwTogglePause`). 시간 경과 실측은 미수행 |
| **D8** | 시트 오류가 시작 전에 잡힌다 | ⚠️ **부분** | `validateSheets` 구현됨. 실제 시트로 3종 오류 재현은 미수행 |

## Metric 게이트

| ID | 목표 | 결과 | 증거 |
|---|---|---|---|
| **M-6** | 최대 배당 16배 이하 | ✅ | `15.2배` (시드 5였다면 29.6배) |
| **M-7** | 난이도별 힌트 6개 이상 | ✅ | `어려움 6 / 중간 6 / 쉬움 6` |
| **M-9** | 힌트 검산 1초 이내 | ✅ | 8! 전수 검사 `0ms` |
| **M-1** | 상태 조회 0.3초 이하 | ⛔ **미검증** | 실제 앱스 스크립트 배포 필요 |
| **M-2~5, M-8** | 라운드 7분 · 1차시 3라운드 · JSON 15KB · 준비 15분 | ⛔ **미검증** | 실제 수업/배포 필요 |

## Rubric

전부 **첫 수업 후 판정**. 다만 시뮬레이션에서 눈에 띈 게 하나 있습니다.

> **R-4 (초반에 코인을 쓰는가)** — 시뮬레이션에서 **힌트를 하나도 못 받은 5모둠이 45코인으로 우승**했습니다.
> 모둠이 기계적으로 베팅한 결과라 그대로 받아들일 건 아니지만, 리뷰 때 지적했던 **C1(최종 배당률만 쓰면
> 운이 지식을 이긴다)** 이 실제로 나타나는 모습입니다. 첫 수업에서 이 항목을 눈여겨보시고, 그렇다면
> `01-prd.md §6-2`의 (가)~(다) 중 하나로 조정하면 됩니다.

---

## 판정

**조건부 승인.** 코드로 확인 가능한 게이트는 **31/31 통과**했습니다. 다만 아래는 **실제 배포 없이는 확인 불가**이며, 이것들이 남아 있는 한 "완료"는 아닙니다.

| 남은 것 | 왜 지금 못 하나 | 언제 |
|---|---|---|
| **H10 학교망 접속** | 학교 와이파이가 필요 | 배포 직후 폰으로 1회 |
| **M-1 응답 속도** | 실제 앱스 스크립트 서버 필요 | 배포 후 폰 6대 |
| **M-2~4 시간 배분** | 실제 수업 필요 | 첫 수업 |
| **D7·D8 실측** | 실제 시트·시계 필요 | 배포 후 리허설 |
| **Rubric R-1~5** | 학생이 필요 | 첫 수업 |

**H10이 가장 위험합니다.** 학교망에서 `script.google.com`이 막히면 스택 자체를 다시 봐야 하므로, 다른 무엇보다 이걸 먼저 확인하시길 권합니다.

---

## 구현 요약 (이해 부채 방지)

| 파일 | 줄 수(대략) | 하는 일 |
|---|---|---|
| `Config.gs` | 90 | 상수·탭 이름·오류 메시지 |
| `Game.gs` | 330 | **게임 규칙 전부. 순수 함수** — 역산·힌트·배당·정산·문제배정 |
| `Sheet.gs` | 250 | 시트 읽기·캐시·잠금·사건 기록·복구 |
| `Code.gs` | 380 | doGet + GameGateway 12개 함수 + 뷰 변환 |
| `Setup.gs` | 110 | 시트 자동 생성 + 샘플 문제 |
| `Test.gs` | 130 | 앱스 스크립트 안에서 돌리는 검사 10개 |
| `Shared.html` | 130 | 공통 CSS·폴러·확인대화 |
| `Teacher.html` | 290 | S1 시작 / S2 진행 / S3 정산 |
| `Team.html` | 300 | S4 접속 / S5 게임(3탭) / S6 결과 |

**핵심 결정 두 가지**

1. `Game.gs`를 순수 함수로 격리해서 **앱스 스크립트 밖(node)에서 그대로 테스트**할 수 있게 했습니다. 앱스 스크립트에는 테스트 도구가 없어서, 이게 아니었으면 H1~H3를 확인할 방법이 없었습니다.
2. 사건을 **즉시 기록**하고 캐시 소실 시 재생해 복구합니다. 라운드 끝에 몰아 쓰는 원래 설계였다면 캐시가 날아갈 때 그 라운드가 통째로 증발했을 겁니다.

**가장 조심할 곳**: `Code.gs`의 `teamView()`. 여기서 필드 하나만 잘못 흘리면 학생이 개발자 도구로 정답을 봅니다. `H4`가 이걸 지킵니다 — 고칠 때마다 검사를 다시 돌리세요.
