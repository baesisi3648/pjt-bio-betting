# 최종 기획 승인 — 와일드 더비

- 라운드: 1 / 최대 3
- 판정: **Ready with Risks** — 개발 착수 가능
- 일자: 2026-08-01

---

## 판정

| 항목 | 값 |
|---|---|
| 품질 점수 | **63 / 65 (96.9%)** |
| 갭 | 5개 발견 → **5개 전부 해소** |
| 게이트 파생 | ✅ REQ 13개 → Hard 10 / Metric 9 / Domain 11 / Rubric 5 |
| 끊긴 추적 체인 | **0개** |
| 잔존 리스크 | 5개 (전부 완화책 있음) |

---

## 이번 라운드에서 고친 것

| 갭 | 내용 | 조치 |
|---|---|---|
| 🔴 G-02 | 동시 진행으로 바꾼 뒤 라운드가 4분이 됐는데 2차시 분할 결정이 그대로 남아 있었음. 게다가 **힌트를 놓고 이야기할 시간이 흐름 어디에도 없었음** | **토론 180초를 정식 단계로 추가** → 라운드 7분 → 2차시 근거 회복. 토론 중에는 힌트 탭이 열리고 베팅이 잠긴다 |
| 🟠 G-01 | `usedQuestions` 필드와 "앞 라운드 문제 재사용" 약속은 있는데 **구현 태스크가 없었음** | `P2-R4-T4 문제 선택기` 추가. 판 생성 시 `questionPlan`에 미리 배정 (캐시 복구 시 문제가 바뀌지 않게) |
| 🟠 G-03 | 힌트 풀이 난이도별 3개인데 "6라운드 중복 금지"를 요구 — **동시에 만족 불가** | 난이도별 **최소 6개** 명시. 같은 틀을 다른 동물에 적용해 채운다 |
| 🟡 G-04 | 태스크가 부르는 Must가 4개뿐 (나머지는 화면 ID 경유) | 태스크 12개에 Must ID 추가 |
| 🟡 G-05 | QR이 "외부 라이브러리 금지"와 충돌, 미결 | **QR 제외**, 주소를 48px 이상으로. 어차피 판 코드는 손으로 입력해야 함 |

가장 값진 발견은 G-02다. 두 결정(2차시 분할 / 모둠 폰 동시 진행)이 **각각은 맞는데 함께 놓으니 앞의 근거가 사라져 있었다.** 그리고 그걸 파고들다 **이 도구의 목적인 "모둠이 이야기하는 시간"이 흐름에 아예 없었다**는 게 드러났다.

---

## 시작 지점

> **P2 (게임 규칙 코어)부터.**

H1~H3-b(도착 순서 일치 / 힌트 참 / 추론 가능)를 통과하기 전에는 화면을 만들 이유가 없다. 여기가 PDF 원본이 깨져 있던 곳이고, 여기만 맞으면 나머지는 평범한 화면 작업이다.

권장 순서: `P0 → P1 → P2(게이트 통과 확인) → P3 → P4·P5 → P6`

---

## 잔존 리스크

| # | 리스크 | 게이트 | 완화 |
|---|---|---|---|
| RR-1 | **학교망에서 앱스 스크립트가 막히면** 스택 자체를 다시 봐야 함 | H10 | P6 전에 미리 폰으로 한 번 확인 |
| RR-2 | 최종 배당률만 쓰면 마지막 몰빵이 최적 전략일 수 있음 (선생님 결정) | R-4 | `bets`에 라운드 번호 보존 → 첫 수업 후 전환 가능 |
| RR-3 | 폰 1대에 4~5명은 기술로 못 푸는 제약 | R-5 | 힌트 20px + 토론 단계 + A4 병행(운영) |
| RR-4 | 동시성을 브라우저 창 6개로 완전히 재현 못 할 수 있음 | H7 | 첫 수업에서 코인 합계 직접 대조 |
| RR-5 | 라운드 7분·1차시 3라운드는 계산값이지 실측이 아님 | M-2, M-3 | 첫 수업에서 `설정` 탭 값 조정 |

---

## 완료의 정의가 바뀌었다

이제부터 "구현 완료"는 이걸 뜻한다:

> `docs/planning/loop/08-derived-gates.md`의 **Hard 10개 + Metric 9개 + Domain 11개**를
> **증거와 함께** 통과한 상태.

구현자의 "됐습니다"는 증거가 아니다. `Test.gs` 실행 로그 원문, 실측 숫자, 재현 절차가 증거다.
